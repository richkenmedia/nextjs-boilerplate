import React from 'react';
import { ButtonDropdown as RButtonDropdown } from 'reactstrap';

const ButtonDropdown = (props) => {
  return <RButtonDropdown {...props} />;
};
export default ButtonDropdown;

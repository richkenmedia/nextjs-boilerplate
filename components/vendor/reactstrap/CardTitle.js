import React from 'react';
import { CardTitle as RCardTitle } from 'reactstrap';

const CardTitle = (props) => {
  return <RCardTitle {...props} />;
};

CardTitle.propTypes = {};

export default CardTitle;

import React from 'react';
import { CardText as RCardText } from 'reactstrap';

const CardText = (props) => {
  return <RCardText {...props} />;
};

CardText.propTypes = {};

export default CardText;
